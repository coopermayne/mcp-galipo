"""
Activity/time tracking routes.

Note: Activity routes are not currently exposed in the original routes.py,
but this module is prepared for future implementation.
"""


def register_activity_routes(mcp):
    """Register activity/time tracking routes.

    Currently a placeholder for future activity tracking API endpoints.
    Activities are managed through MCP tools but could be exposed here for the web UI.
    """
    # Activity routes would go here when needed
    # For now, activities are managed through MCP tools (tools.py)
    pass
